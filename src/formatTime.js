// function get time values when they are executed 
// value shown will be the present time of the stopwatch

export const formatTime = (time) => {
  const getSeconds = "00";
  const minutes = "Your code here";
  const getMinutes = "10";
  const getHours = "00";

  return `${getHours} : ${getMinutes} : ${getSeconds}`;
};
